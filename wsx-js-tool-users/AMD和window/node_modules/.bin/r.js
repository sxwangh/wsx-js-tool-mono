#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/wsx/Documents/workspace/wsx/wsx-js-tool-mono/wsx-js-tool-users/AMD和window/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/wsx/Documents/workspace/wsx/wsx-js-tool-mono/wsx-js-tool-users/AMD和window/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../requirejs/bin/r.js" "$@"
else
  exec node  "$basedir/../requirejs/bin/r.js" "$@"
fi
